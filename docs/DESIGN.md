# UI 设计规范（DESIGN.md）

> 本文件是**可修改的活文档**：视觉约定以本文件为准，代码侧只落地在 `frontend/src/theme/` 一处。
> 修改本文件后，只需同步 token 文件即可全局生效，不应出现散落各页面的硬编码色值。

---

## 1. 设计原则

1. **信息结构优先于视觉**：先把信息层级、分区、交互路径理清，再谈配色与动效。
2. **风险语义色唯一且稳定**：低/中/高与四种决策动作的颜色在全系统内不得改变含义。
3. **状态完整**：每个数据区块必须具备加载态、空态、错误态、成功反馈，禁止"白屏即无数据"。
4. **密度适中**：风控系统以表格与图表为主，默认紧凑但不拥挤，一屏尽量少滚动。

---

## 2. 色彩

### 2.1 主色与功能色

| 用途 | 色值 | 说明 |
| --- | --- | --- |
| 主色 primary | `#1677ff` | 主按钮、链接、选中态、图表主序列 |
| 成功 success | `#52c41a` | 放行、处置成功 |
| 警告 warning | `#faad14` | 中风险、待办提醒 |
| 危险 danger | `#ff4d4f` | 高风险、拦截、删除类操作 |
| 信息 info | `#1677ff` | 常规提示 |

### 2.2 风险等级语义色（禁止复用为其他含义）

| 等级 | 色值 | 应用 |
| --- | --- | --- |
| 低风险 low | `#52c41a` | 标签、分布图、评分徽标 |
| 中风险 mid | `#faad14` | 标签、分布图、评分徽标 |
| 高风险 high | `#ff4d4f` | 标签、分布图、评分徽标 |

### 2.3 决策动作语义色

| 动作 | 色值 | 底色（标签） |
| --- | --- | --- |
| Pass 放行 | `#52c41a` | `#f6ffed` |
| Challenge 挑战 | `#1677ff` | `#e6f4ff` |
| Review 人审 | `#faad14` | `#fffbe6` |
| Reject 拦截 | `#ff4d4f` | `#fff2f0` |

### 2.4 中性色

| 用途 | 色值 |
| --- | --- |
| 主文本 | `#1f2329` |
| 次文本 | `#646a73` |
| 禁用/占位 | `#8f959e` |
| 边框 | `#e5e6eb` |
| 分割线 | `#f0f0f0` |
| 页面背景 | `#f5f6f7` |
| 卡片背景 | `#ffffff` |

### 2.5 图表配色（ECharts 序列顺序）

```
#1677ff, #52c41a, #faad14, #ff4d4f, #722ed1, #13c2c2, #eb2f96, #fa8c16
```

- 折线图：主序列 `#1677ff`，面积渐变 `rgba(22,119,255,0.18) → 透明`。
- 环形图：固定使用"低/中/高"语义色，不按序列取色。
- 排行榜条形图：统一主色，Top 3 使用主色加深。

---

## 3. 字体与字号

字体栈：`-apple-system, "Segoe UI", "Microsoft YaHei", "PingFang SC", sans-serif`
数字与编码类字段（案件号、事件号、IP、设备ID）：`Consolas, "JetBrains Mono", monospace`

| 层级 | 字号 | 字重 | 用途 |
| --- | --- | --- | --- |
| 页面标题 | 20px | 600 | 页面顶部 |
| 卡片标题 | 16px | 600 | 卡片、区块头 |
| 正文 | 14px | 400 | 表格、表单、描述 |
| 辅助 | 12px | 400 | 说明、时间戳、次要标签 |
| 指标数值 | 30px | 600 | 大盘指标卡 |

---

## 4. 间距与栅格

- 间距以 **4px** 为基础单位：`4 / 8 / 12 / 16 / 24 / 32`。
- 页面内容区内边距 `24px`；卡片间距 `16px`；卡片内边距 `16px 20px`；表单项纵向间距 `16px`。
- 主框架：左侧导航宽 `220px`（折叠 `64px`），顶栏高 `56px`，内容区自适应。
- 审核工作台三栏：左 `320px` ｜ 中 自适应（min 480px）｜ 右 `400px`；中栏与右栏在 <1440px 视口下允许折叠为 Drawer。

---

## 5. 布局规范

- 每页结构：`页面标题 + 面包屑` → `筛选/操作区` → `内容区`。
- 卡片统一圆角 `8px`，阴影 `0 1px 2px rgba(0,0,0,0.04)`，不使用重阴影。
- 大盘采用 24 栅格：指标卡 `6` 列（4 张一行），趋势图 `16` 列 + 分布图 `8` 列，排行 `12` 列 + 滚屏 `12` 列。
- 表格默认 `size="middle"`，分页 `20 / 50 / 100`；长文本省略并支持 Tooltip。

---

## 6. 组件使用约定

| 场景 | 约定 |
| --- | --- |
| 风险等级/动作 | 统一 `Tag`，使用 §2.2/§2.3 色板，文案固定为"低/中/高风险""放行/挑战/人审/拦截" |
| 数值指标 | `Statistic`，数值 30px，单位与说明置于下方 12px 次文本 |
| 详情字段 | `Descriptions` 两列（窄屏一列），label 次文本色 |
| 二次确认 | `Modal.confirm`，危险操作按钮使用 danger 样式 |
| 表单 | `Form` 垂直布局，必填标星，校验信息即时展示 |
| 条件编辑器 | 树形节点 `与/或/非 + 条件行`，条件行 = 字段下拉 + 运算符下拉 + 值输入，未知字段标红 |
| 实体图谱 | ECharts `graph` 力导向，容器高度 `420px`，节点按风险等级取色，边按关系类型区分虚实 |
| 链路回溯 | `Steps` 竖向五步（特征 → 名单 → 规则 → 模型 → 决策），每步可展开明细 |
| 状态标签 | 案件状态：待审 `gold`、审核中 `blue`、已处置 `green`、已归档 `default`、已关闭 `default` |

---

## 7. 状态反馈规范

| 状态 | 表现 |
| --- | --- |
| 加载 | 骨架屏 `Skeleton`（卡片/表格）或 `Spin`（局部） |
| 空态 | `Empty`，附一句可执行建议（如"暂无案件，可在仿真页注入事件"） |
| 错误 | `Result status="error"`，给出原因与重试按钮 |
| 成功 | 顶部 `message.success`，措辞具体（如"已拦截订单 ORD-2026-0001"） |
| 实时断线 | 顶栏常驻提示条"实时推送已断开，正在重连…"，恢复后提示"已恢复" |

---

## 8. 文案与交互细节

- 全站中文，动词前置："接手案件""提交处置""导出名单"。
- 危险操作确认文案模板：`确定要{动作}吗？该操作将{影响后果}，且会记入审计日志。`
- 批量导入反馈：成功 N 条 / 失败 M 条，失败行给出原因并可下载失败明细。
- 时间显示统一 `YYYY-MM-DD HH:mm:ss`（Asia/Shanghai），悬浮显示相对时间。

---

## 9. 主题落地（与代码对应）

| 规范项 | 落地位置 |
| --- | --- |
| 主色/功能色 | `frontend/src/theme/tokens.ts` → `ConfigProvider.theme.token.colorPrimary` 等 |
| 风险语义色 | `theme/semantic.ts` 导出 `RISK_COLORS`、`ACTION_COLORS`，全站引用，禁止硬编码 |
| 图表色板 | `theme/charts.ts` 导出 `CHART_PALETTE` 并注册 ECharts 主题 |
| 布局尺寸 | `theme/layout.ts` 常量（侧边栏宽度、三栏宽度、图谱高度） |
