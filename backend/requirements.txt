# 电商风险控制系统 —— 后端依赖（P0）
# 说明：
#   1) 采用「宽下界 + 上界」约束而非精确锁版本，首次安装成功后会导出 requirements.lock.txt 作为可复现快照。
#   2) 训练相关依赖（numpy / scikit-learn）只被离线脚本使用，不进入线上请求路径。

# ---- Web 与校验 ----
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0

# ---- 数据访问 ----
SQLAlchemy>=2.0,<2.1
alembic>=1.13,<2.0
PyMySQL>=1.1,<2.0
cryptography>=43,<46            # MySQL 8 默认 caching_sha2_password 认证需要
redis>=5.0,<6.0

# ---- 鉴权与序列化 ----
PyJWT>=2.9,<3.0
bcrypt>=4.2,<5.0
orjson>=3.10,<4.0

# ---- 开发与测试 ----
httpx>=0.27,<1.0
pytest>=8.3,<9.0
pytest-asyncio>=0.24,<1.0
pytest-timeout>=2.3,<3.0      # 防住"等数据库锁"这类无声挂死：无超时的测试会一直卡住不报错

# ---- 离线训练（脚本专用）----
numpy>=2.0,<3.0
scikit-learn>=1.5,<2.0
