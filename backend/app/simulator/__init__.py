"""模拟业务端（docs/PRD.md §6.2）。

**为什么要内建一个业务端**：风控系统最容易被演示成"一个只会自己说话的接口"——
注入事件、返回决策、结束。但风控的真实价值是**影响业务动作**：
Reject 之后券没发出去、订单被拦下、退款被驳回。没有业务端，
这套系统就只是个打分器，PRD §9.4 的"处置联动"也无处落地。

职责边界：

* 本包只做"业务侧"的事 —— 生成业务单据、按决策结果决定是否放行；
* 决策本身全部委托给 ``app/services/event_gateway``，
  与真实业务系统接入风控的方式完全一致（同一份事件契约、同一条链路）。

因此它同时是"演示脚手架"和"接入样例"：任何第三方系统接本项目，
照着 :class:`app.simulator.service.BusinessSimulator` 的调用方式即可。
"""
